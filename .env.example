# Optional: only used by local debug scripts (`npm run chrome`, `npm run inspect`).
# The extension itself uses whichever https://*.atlassian.net site you have open.
# No trailing slash.
JIRA_ORIGIN=https://your-site.atlassian.net
